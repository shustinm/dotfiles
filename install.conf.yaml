- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.zprofile: zprofile
    ~/.zshrc: zshrc
    ~/.config/:
      glob: true
      path: config/**
      relink: true
    ~/.btt_autoload_preset.json: btt/autoload.bttpreset
    ~/Brewfile: Brewfile


- create:
  - ~/Developer
  - ~/.config

